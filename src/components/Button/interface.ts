// export interface listType {
// 	name: string
// 	filterProperty: string
// }

export interface ButtonProps {
	children?: React.ReactNode
	name: string
	tag?: string
	path?: string
	onClick?: () => void
}
